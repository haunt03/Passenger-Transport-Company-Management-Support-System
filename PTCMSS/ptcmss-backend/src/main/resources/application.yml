spring:
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    properties:
      hibernate:
        physical_naming_strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  sql:
    init:
      mode: always

  mail:
    host: smtp.gmail.com
    port: 587
    username: hethonghotroquanlycongtyvantai@gmail.com
    password: dhkn hjag btur xwcf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# Keys / API (đặt ENV để tránh lộ)
serpapi:
  key: ${SERPAPI_KEY:d403ede6a15a2684e960ee9be9eb913a534330bf909263934657741c7f326742}

graphhopper:
  api-key: ${GRAPHHOPPER_KEY:56f6f3a5-1958-408c-8788-225991ec1d3d}

payment:
  qr:
    provider-url: https://img.vietqr.io/image
    bank-code: ${PAYMENT_BANK_CODE:970423}
    account-number: ${PAYMENT_BANK_ACCOUNT_NUMBER:00005505543}
    account-name: ${PAYMENT_BANK_ACCOUNT_NAME:NGUYEN THUY HAU}
    template: compact
    description-prefix: PTCMSS
    expires-in-minutes: 120

springdoc:
  api-docs:
    enabled: ${SPRINGDOC_ENABLED:true}
    path: /v3/api-docs
  swagger-ui:
    enabled: ${SWAGGER_UI_ENABLED:true}
    path: /swagger-ui.html

openapi:
  service:
    api-docs: ${OPENAPI_API_DOCS:PTCMSS}
    server: ${OPENAPI_SERVER:http://localhost:8080}
    title: ${OPENAPI_TITLE:Transport Application}
    version: ${OPENAPI_VERSION:1.0.0}

jwt:
  expriMinutes: 3600
  expireDate: 5
  accesskey: ${JWT_ACCESS_KEY}
  refreshkey: ${JWT_REFRESH_KEY}
server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    mail:
      enabled: ${MANAGEMENT_HEALTH_MAIL_ENABLED:false}

logging:
  level:
    org.thymeleaf: ${THYMELEAF_LOG_LEVEL:WARN}

app:
  base-url: ${APP_BASE_URL:http://localhost:8080}
  frontend-url: ${APP_FRONTEND_URL:http://localhost:5173}
