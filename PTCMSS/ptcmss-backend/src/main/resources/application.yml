spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ptcmss_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
      naming:
        # Dùng đúng tên bạn khai báo/annotate, KHÔNG tự convert sang snake_case
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

  mail:
    host: smtp.gmail.com
    port: 587
    username: macuongthi012@gmail.com
    password: mdgkuccoaggtxlua
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  profiles:
    active: dev

openapi:
  service:
    api-docs: Computer_sell
    server: http://localhost:8080
    title: Transport Application
    version: 1.0.0

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

# Payment Configuration
payment:
  bank:
    code: ${PAYMENT_BANK_CODE:970418}  # Mã ngân hàng (970418 = Vietcombank)
    account:
      number: ${PAYMENT_BANK_ACCOUNT_NUMBER:1234567890}  # Số tài khoản nhận tiền
      name: ${PAYMENT_BANK_ACCOUNT_NAME:CONG TY VAN TAI HANH KHACH}  # Tên chủ tài khoản

# VietQR API Configuration
vietqr:
  client-id: ${VIETQR_CLIENT_ID:}  # Lấy từ https://my.vietqr.io/profile
  api-key: ${VIETQR_API_KEY:}      # Lấy từ https://my.vietqr.io/profile