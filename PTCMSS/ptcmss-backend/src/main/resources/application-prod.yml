spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ptcmss_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true  # IMPORTANT: Run data.sql AFTER Hibernate creates tables
  sql:
    init:
      mode: always  # Run data.sql on startup
      continue-on-error: false  # Stop if SQL script fails
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:hethonghotroquanlycongtyvantai@gmail.com}
    password: ${MAIL_PASSWORD:fzzqyukjsbvegvym}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8080
  address: 0.0.0.0  # Lắng nghe trên tất cả interfaces, không chỉ localhost
management:
  endpoints:
    web.exposure.include: health,info
jwt:
  expriMinutes: 3600
  expireDate: 5
  accesskey: 2mfGKBhdCa0K8X2uV1Fzn9o6sR8V0s5GUelGlxsFQAc=
  refreshkey: /AqIfLpfbWqy36D3VBmkskYtmnpsJrLpRx00WVRnJlE=

# Application URLs for email links and redirects
app:
  base-url: ${APP_BASE_URL:https://api.hethongvantai.site}
  frontend-url: ${APP_FRONTEND_URL:https://hethongvantai.site}

# Logging configuration
logging:
  level:
    org.thymeleaf: WARN  # Giảm log level của Thymeleaf để tránh ERROR khi template không tìm thấy (fallback sẽ xử lý)
